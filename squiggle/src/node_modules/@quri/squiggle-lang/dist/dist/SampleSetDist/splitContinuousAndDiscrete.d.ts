type splitSamples = {
    continuousSamples: number[];
    discreteShape: {
        xs: number[];
        ys: number[];
    };
};
export declare const splitContinuousAndDiscrete: (sortedArray: readonly number[], minDiscreteWeight: number) => splitSamples;
export declare const continuousAreSameFilter: ({ continuousSamples, discreteShape: { xs, ys }, }: splitSamples) => splitSamples;
export declare const minContinuousSamplesFilter: (minSampleCount: number, { continuousSamples, discreteShape }: splitSamples) => splitSamples;
export {};
//# sourceMappingURL=splitContinuousAndDiscrete.d.ts.map