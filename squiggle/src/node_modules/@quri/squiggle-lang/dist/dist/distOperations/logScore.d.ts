import { result } from "../../utility/result.js";
import { BaseDist } from "../BaseDist.js";
import { DistError } from "../DistError.js";
import { Env } from "../env.js";
export declare function logScoreDistAnswer({ estimate, answer, prior, env, }: {
    estimate: BaseDist;
    answer: BaseDist;
    prior: BaseDist | undefined;
    env: Env;
}): result<number, DistError>;
export declare function logScoreScalarAnswer({ estimate, answer, prior, env, }: {
    estimate: BaseDist;
    answer: number;
    prior: BaseDist | undefined;
    env: Env;
}): result<number, DistError>;
//# sourceMappingURL=logScore.d.ts.map