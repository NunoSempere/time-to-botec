import { OperationError } from "../operationError.js";
import * as Result from "../utility/result.js";
import * as Mixed from "./Mixed.js";
import { MixedPoint } from "./MixedPoint.js";
import { AnyPointSet } from "./PointSet.js";
export declare const WithDistAnswer: {
    integrand(estimateElement: number, answerElement: number): Result.result<number, OperationError>;
    sum({ estimate, answer, }: {
        estimate: AnyPointSet;
        answer: AnyPointSet;
    }): Result.result<number, OperationError>;
    sumWithPrior({ estimate, answer, prior, }: {
        estimate: AnyPointSet;
        answer: AnyPointSet;
        prior: AnyPointSet;
    }): Result.result<number, OperationError>;
};
export declare const WithScalarAnswer: {
    sum(mp: MixedPoint): number;
    score({ estimate, answer, }: {
        estimate: Mixed.MixedShape;
        answer: number;
    }): Result.result<number, OperationError>;
    scoreWithPrior({ estimate, answer, prior, }: {
        estimate: Mixed.MixedShape;
        answer: number;
        prior: Mixed.MixedShape;
    }): Result.result<number, OperationError>;
};
//# sourceMappingURL=PointSetDist_Scoring.d.ts.map