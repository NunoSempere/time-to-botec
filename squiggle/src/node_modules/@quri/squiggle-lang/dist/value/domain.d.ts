import { Value } from "./index.js";
declare abstract class BaseDomain {
    abstract type: string;
    abstract toString(): string;
    abstract includes(value: Value): boolean;
}
export declare class NumericRangeDomain extends BaseDomain {
    min: number;
    max: number;
    readonly type = "NumericRange";
    constructor(min: number, max: number);
    toString(): string;
    includes(value: Value): boolean;
    isEqual(other: NumericRangeDomain): boolean;
}
export type Domain = NumericRangeDomain;
export declare function annotationToDomain(value: Value): Domain;
export {};
//# sourceMappingURL=domain.d.ts.map